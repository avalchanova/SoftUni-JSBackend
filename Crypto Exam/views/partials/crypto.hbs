<div class="coin">
    <div class="coin-img">
        <img src="{{image}}">
    </div>
    <div class="coin-info">
        <h1>{{name}}</h1>
        <p><span>Price: </span>${{price}}</p>
        <p><span>Payment: </span>{{paymentMethod}}</p>
    </div>

    <a href="/crypto/{{_id}}/details" class="btn-details">Details</a>
    {{!-- only _id because we are in EACH CRYPTO which means we will extract the id from the given crypto
    --}}
</div>